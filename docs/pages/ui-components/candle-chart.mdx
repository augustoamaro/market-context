# Candle Chart

Arquivo: `app/components/CandleChart.tsx`.

- Usa TradingView Lightweight Charts (v5)
- Endpoint: `/api/candles`
- Formato: `time`, `open`, `high`, `low`, `close`
- Render client-side (SSR-safe)
