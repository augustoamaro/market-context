# Regime Hero Card

Arquivo: `app/components/RegimeHeroCard.tsx`.

Mostra o resumo macro do contexto ativo:

- regime atual (`expansion`/`equilibrium`)
- motivo textual (`stateReason`)
- métricas rápidas (RSI, volume ratio, MACD)
