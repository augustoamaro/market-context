# Trend Classification

A tendência vem do stack de EMAs:

- `up`: `ema20 > ema50 > ema200`
- `down`: `ema20 < ema50 < ema200`
- `sideways`: qualquer outra configuração

A mesma lógica é reaproveitada em `deriveAlignment` para o monitor multi-timeframe.
